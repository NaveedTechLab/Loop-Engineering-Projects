# Agent Instructions

This project's only task is running the `quick-check` skill and reporting
its timestamp honestly. This is a rehearsal project, not a real workload —
never invent a timestamp; always run the script and report what it
actually printed.
